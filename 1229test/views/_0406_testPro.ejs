<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <pre>
        [문제] 
            1 에서 300 사이의 숫자중 다음 조건에 전부 맞는 숫자를 출력하시오.
            [조건1] 11의 배수를 출력
            [조건2] 200에 가장가까운 수를 출력 
        
    </pre>

    <% 
    var first = 0
    var second =  0
    %>

    <%for(i=1;i<=300;i++){%>
        <%if(i % 11 == 0 && i <200){%>
            <%first = i%>
        <%}%>
    <%}%>
    
    <%=first %>

    <%second =first+11%>
    
    <%=second %>

    <%var first_diff = 200 - first
    var second_diff = second -200
    %>

    <%if (first_diff>second_diff){%>
            <%=second%>    
    <%}%>
    <%if (second_diff>first_diff){%>
        <br>
        <%="정답은 "%>    
        <%=first%>
    <%}%>
    %> 
    <a href="main">돌아가기</a>
</body>
</html>